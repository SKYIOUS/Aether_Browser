[package]
name = "aether-css"
version = "0.1.0"
edition = "2021"

[dependencies]
strum = { version = "0.26", features = ["derive"] }

[build-dependencies]
serde_json = "1"

[features]
default = ["content_size"]
content_size = []
