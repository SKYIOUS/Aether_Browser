# JavaScript Runtime

Phase 7 implements a basic JS runtime interface for the Aether Browser.

## Design
- `engine::js` provides a wrapper for script execution.
- Currently a placeholder, intended for integration with a library like `quickjs-rs`.
